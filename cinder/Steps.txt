1. Specify the required variables in cinder_info.yml (self-explanatory) and add public IPs in [hostList] and [cephNode] in inventory_cinder file.
2. ansible-playbook -v deploy_cinder.yaml -i vars/inventory_cinder -u block_team
